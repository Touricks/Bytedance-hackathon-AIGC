export function VideoScriptStep({
  workspaceId,
  shotId,
}: {
  workspaceId: string;
  shotId: string;
}) {
  return (
    <div>
      Video script step (Task 13) for {shotId} / {workspaceId}
    </div>
  );
}
