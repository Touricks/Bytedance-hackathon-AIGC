export function ReviewStep({
  workspaceId,
  shotId,
}: {
  workspaceId: string;
  shotId: string;
}) {
  return (
    <div>
      Review step (Task 15) for {shotId} / {workspaceId}
    </div>
  );
}
