export function ImageCandidatesStep({
  workspaceId,
  shotId,
}: {
  workspaceId: string;
  shotId: string;
}) {
  return (
    <div>
      Image candidates step (Task 12) for {shotId} / {workspaceId}
    </div>
  );
}
