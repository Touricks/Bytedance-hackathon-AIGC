export function VideoCandidatesStep({
  workspaceId,
  shotId,
}: {
  workspaceId: string;
  shotId: string;
}) {
  return (
    <div>
      Video candidates step (Task 14) for {shotId} / {workspaceId}
    </div>
  );
}
