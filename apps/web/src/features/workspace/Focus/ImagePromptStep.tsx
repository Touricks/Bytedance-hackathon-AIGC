export function ImagePromptStep({
  workspaceId,
  shotId,
}: {
  workspaceId: string;
  shotId: string;
}) {
  return (
    <div>
      Image prompt step (Task 11) for {shotId} / {workspaceId}
    </div>
  );
}
