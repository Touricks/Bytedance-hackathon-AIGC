# Demo Videos

Store 1-2 pre-generated demo videos here before the final presentation.

The current scaffold falls back to a public sample video through `MOCK_FINAL_VIDEO_URL`.
Replace it with a real generated asset, for example:

```env
MOCK_FINAL_VIDEO_URL=/mocks/videos/demo.mp4
```
